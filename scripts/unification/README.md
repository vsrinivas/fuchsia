# Build unification tools

This directory contains various helper tools for the build unification project
(turning the ZN build and the Fuchsia build into a single build).

When the project is over, the entire directory may be deleted.
