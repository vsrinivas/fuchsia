[package]
name = "mesh-endpoint"
version = "0.1.0"
authors = ["Craig Tiller <ctiller@google.com>"]

[dependencies]
mesh-protocol = "*"
mesh-router = "*"
futures = { version = "0.2.1", features = ["nightly"] }
failure = "*"
flatten-future-sink = "*"
bytes = "*"
