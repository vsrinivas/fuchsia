analyzer:
  strong-mode: true
  errors:
    # Implicit down casts.  These are only injected by the compiler by flag.
    #
    # A down cast to a non-ground type.  These behave differently from standard
    # Dart and may be more likely to fail at runtime.
    strong_mode_down_cast_composite: ignore
    # we allow having TODOs in the code
    todo: ignore
    # This analysis is broken right now:
    # https://github.com/dart-lang/sdk/issues/26141
    const_constructor_param_type_mismatch: ignore
    const_constructor_field_type_mismatch: ignore
