# Fuchsia accessibility virtual-keyboard

`fuchsia.accessibility.virtualkeyboard` provides definitions needed to inform
accessibility of virtual keyboard state (e.g. whether the virtual keyboard
is visible).