# Connectivity debugging
net-cli
log_listener
wlan
iquery
curl

# DevTools
system_monitor_harvester

# Other debugging tools
run

# Needed by infrastructure until they convert to new devtools
ls
cat
uname
rm
pkgctl
amberctl

# Needed to load trace specs for tests
benchmark
dart-inspect-benchmarks
fidl_benchmarks
fuchsia_benchmarks
garnet_input_latency_benchmarks
go_fidl_benchmarks
netstack_benchmarks
rust_inspect_benchmarks

# Needed by the peridot tests
catapult_converter

# remote-control is a v2 component that is cached (not in base)
remote-control