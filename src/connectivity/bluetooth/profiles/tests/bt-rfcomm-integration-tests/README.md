# RFCOMM Integration Tests

## Build Configuration

Include in your build args via `fx set` or `fx args`:

`//src/connectivity/bluetooth/profiles/tests`

To run the RFCOMM component integration tests:

`fx test bt-rfcomm-integration-tests`
