# A2DP Source Integration Tests

## Build Configuration

Include `--with //src/connectivity/bluetooth/profiles/tests/bt-a2dp-source-integration-tests` in your `fx set`.

To run the integration tests:

`fx test bt-a2dp-source-integration-tests`