Proc Macros for `spinel_pack`
===============================

This crate provides the proc-macros for the `spinel_pack` crate.

**Don't use this crate directly**, use the `spinel_pack` crate instead.
