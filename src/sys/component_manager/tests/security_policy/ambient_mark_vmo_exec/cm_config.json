{
    debug: true,
    security_policy: {
        job_policy: {
            ambient_mark_vmo_exec: [
                "/policy_allowed",
            ],
        }
    },
    builtin_pkg_resolver: "appmgr_bridge",
}
