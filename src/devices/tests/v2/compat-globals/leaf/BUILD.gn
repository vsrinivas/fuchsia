# Copyright 2022 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# This file was generated by the `fx create` command. The template
# is located at `//tools/create/templates/driver-default/BUILD.gn.tmpl-cpp`.
# If you find something broken, we are eager to review fixes.

import("//build/bind/bind.gni")
import("//build/components.gni")
import("//build/drivers.gni")
import("//build/test.gni")

driver_bind_rules("bind") {
  rules = "leaf.bind"
  header_output = "leaf-bind.h"
  bind_output = "leaf.bindbc"
  deps = [ "//src/devices/tests/v2/compat/bind:fuchsia.compat" ]
}

fuchsia_driver("driver") {
  output_name = "compat-globals-leaf"
  sources = [ "leaf.cc" ]
  deps = [
    ":bind",
    "//src/devices/lib/driver",
    "//src/devices/tests/v2/compat-globals:fuchsia.hardware.compat_banjo_cpp",
    "//src/devices/tests/v2/compat-globals:fuchsia.hardware.compat_llcpp",
    "//src/devices/tests/v2/compat-globals/bind:bind.fuchsia.compat",
    "//src/lib/ddktl",
    "//zircon/system/ulib/fbl",
    "//zircon/system/ulib/inspect",
    "//zircon/system/ulib/zx",
  ]
}

fuchsia_driver_component("component") {
  component_name = "leaf"
  deps = [ ":driver" ]
  info = "leaf-info.json"
  colocate = true
}
