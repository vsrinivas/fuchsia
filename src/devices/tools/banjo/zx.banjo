library zx;

using status = int32;
using time = int64;
using duration = int64;
using clock = uint32;
using koid = uint64;
using vaddr = uint64;
using paddr = uint64;
using paddr32 = uint32;
using gpaddr = uint64;
using off = uint64;
using rights = uint32;
using signals = uint32;
using vm_option = uint32;
