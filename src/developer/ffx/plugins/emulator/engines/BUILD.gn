# Copyright 2021 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

if (is_host) {
  group("ffx_emulator_engines_args") {
    deps = [ "vdl:ffx_emulator_engines_vdl_args" ]
  }

  group("ffx_emulator_engines") {
    deps = [ "vdl:ffx_emulator_engines_vdl" ]
  }

  group("tests") {
    testonly = true
    deps = [ "vdl:tests($host_toolchain)" ]
  }
}
