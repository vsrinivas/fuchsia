# Hello Pose Buffer Example

This directory contains an application which displays a simple scene and manipulates the camera with a pose buffer. It runs for 50 seconds, then quits.

## USAGE

  hello_pose_buffer
