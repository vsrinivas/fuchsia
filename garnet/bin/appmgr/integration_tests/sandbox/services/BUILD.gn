# Copyright 2019 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# Do not use this target directly, instead depend on //src/sys/appmgr/integration_tests/sandbox/services:multiple_components.
group("multiple_components") {
  public_deps = [
    "//src/sys/appmgr/integration_tests/sandbox/services:multiple_components",
  ]
  testonly = true
}

# Do not use this target directly, instead depend on //src/sys/appmgr/integration_tests/sandbox/services:some_services.
group("some_services") {
  public_deps = [
    "//src/sys/appmgr/integration_tests/sandbox/services:some_services",
  ]
  testonly = true
}

# Do not use this target directly, instead depend on //src/sys/appmgr/integration_tests/sandbox/services:no_services.
group("no_services") {
  public_deps = [
    "//src/sys/appmgr/integration_tests/sandbox/services:no_services",
  ]
  testonly = true
}
