# Copyright 2019 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# Do not use this target directly, instead depend on //src/security/kms:kms.
group("kms") {
  public_deps = [ "//src/security/kms:kms" ]
}

# Do not use this target directly, instead depend on //src/security/kms:kms_tests.
group("kms_tests") {
  testonly = true
  public_deps = [ "//src/security/kms:kms_tests" ]
}

# Do not use this target directly, instead depend on //src/security/kms:optee_provider_config.
group("optee_provider_config") {
  public_deps = [ "//src/security/kms:optee_provider_config" ]
}

# Do not use this target directly, instead depend on //src/security/kms:optee_provider_test_config.
group("optee_provider_test_config") {
  public_deps = [ "//src/security/kms:optee_provider_test_config" ]
}
