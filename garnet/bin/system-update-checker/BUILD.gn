# Copyright 2019 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# Do not use these targets directly, instead depend on their dependencies directly.

group("system-update-checker") {
  public_deps = [
    "//src/sys/pkg/bin/system-update-checker",
  ]
}

group("system-update-checker-tests") {
  public_deps = [
    "//src/sys/pkg/bin/system-update-checker:system-update-checker-tests",
  ]
  testonly = true
}

group("auto_update") {
  public_deps = [
    "//src/sys/pkg/bin/system-update-checker:auto_update",
  ]
}

group("system-update-checker-config") {
  public_deps = [
    "//src/sys/pkg/bin/system-update-checker:system-update-checker-config",
  ]
}
