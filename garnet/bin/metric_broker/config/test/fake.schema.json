{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "Fake Configuration",
    "description": "Fake Schema file for checking base JsonReader class.",
    "type": "object",
    "properties": {
        "requried_field": {
            "description": "Schema required field.",
            "type": "boolean"
        },
        "optional_field": {
            "description": "Schema optional field.",
            "type": "boolean"
        }
    },
    "required": [
        "required_field"
    ]
}