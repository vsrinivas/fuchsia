{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "Inspect Configuration",
    "description": "Inspect Configuration Data.",
    "type": "object",
    "properties": {
        "consistency_check": {
            "description": "Whether the snapshot should be consistent view.",
            "type": "boolean"
        }
    },
    "required": [
        "consistency_check"
    ]
}