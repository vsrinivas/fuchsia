# Copyright 2019 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# Do not use this target directly, instead depend on //src/lib/network_wrapper:unittests.
group("unittests") {
  public_deps = [ "//src/lib/network_wrapper:unittests" ]
  testonly = true
}

# Do not use this target directly, instead depend on //src/lib/network_wrapper:fake.
group("fake") {
  public_deps = [ "//src/lib/network_wrapper:fake" ]
  testonly = true
}

# Do not use this target directly, instead depend on //src/lib/network_wrapper:network_wrapper.
group("network_wrapper") {
  public_deps = [ "//src/lib/network_wrapper:network_wrapper" ]
}
