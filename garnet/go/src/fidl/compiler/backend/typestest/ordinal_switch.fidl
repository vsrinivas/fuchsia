library test.name;

protocol SwitchingOrdinals {
    OrdinalFive(); // ordinal and generated_ordinal are different
    OnlyGeneratedOrdinal(); // ordinal and generated_ordinal are the same
    -> EventOrdinalFifteen(); // ordinal and generated_ordinal are different
    -> EventOnlyGeneratedOrdinal(); // ordinal and generated_ordinal are the same
};
