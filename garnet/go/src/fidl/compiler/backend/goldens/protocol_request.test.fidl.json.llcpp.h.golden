// WARNING: This file is machine generated by fidlgen.

#pragma once

#include <lib/fidl/internal.h>
#include <lib/fidl/cpp/vector_view.h>
#include <lib/fidl/cpp/string_view.h>
#include <lib/fidl/llcpp/array.h>
#include <lib/fidl/llcpp/coding.h>
#include <lib/fidl/llcpp/traits.h>
#include <lib/fidl/llcpp/transaction.h>
#include <lib/zx/channel.h>
#include <zircon/fidl.h>

namespace test {
namespace name {

class Parent;
class Child;

extern "C" const fidl_type_t test_name_ParentGetChildResponseTable;
extern "C" const fidl_type_t test_name_ParentGetChildRequestResponseTable;
extern "C" const fidl_type_t test_name_ParentTakeChildRequestTable;
extern "C" const fidl_type_t test_name_ParentTakeChildRequestRequestTable;

class Parent final {
 public:

  struct GetChildResponse {
    FIDL_ALIGNDECL
    fidl_message_header_t _hdr;
    ::zx::channel c;

    static constexpr const fidl_type_t* Type = &test_name_ParentGetChildResponseTable;
    static constexpr uint32_t MaxNumHandles = 1;
    static constexpr uint32_t PrimarySize = 24;
    static constexpr uint32_t MaxOutOfLine = 0;
  };
  using GetChildRequest = ::fidl::AnyZeroArgMessage;

  struct GetChildRequestResponse {
    FIDL_ALIGNDECL
    fidl_message_header_t _hdr;
    ::zx::channel r;

    static constexpr const fidl_type_t* Type = &test_name_ParentGetChildRequestResponseTable;
    static constexpr uint32_t MaxNumHandles = 1;
    static constexpr uint32_t PrimarySize = 24;
    static constexpr uint32_t MaxOutOfLine = 0;
  };
  using GetChildRequestRequest = ::fidl::AnyZeroArgMessage;

  struct TakeChildRequest {
    FIDL_ALIGNDECL
    fidl_message_header_t _hdr;
    ::zx::channel c;

    static constexpr const fidl_type_t* Type = &test_name_ParentTakeChildRequestTable;
    static constexpr uint32_t MaxNumHandles = 1;
    static constexpr uint32_t PrimarySize = 24;
    static constexpr uint32_t MaxOutOfLine = 0;
  };

  struct TakeChildRequestRequest {
    FIDL_ALIGNDECL
    fidl_message_header_t _hdr;
    ::zx::channel r;

    static constexpr const fidl_type_t* Type = &test_name_ParentTakeChildRequestRequestTable;
    static constexpr uint32_t MaxNumHandles = 1;
    static constexpr uint32_t PrimarySize = 24;
    static constexpr uint32_t MaxOutOfLine = 0;
  };


  class SyncClient final {
   public:
    SyncClient(::zx::channel channel) : channel_(std::move(channel)) {}

    ~SyncClient() {}

    zx_status_t GetChild(::zx::channel* out_c);

    // Caller provides the backing storage for FIDL message via request and response buffers.
    zx_status_t GetChild(::fidl::BytePart _response_buffer, ::zx::channel* out_c);

    // Messages are encoded and decoded in-place.
    ::fidl::DecodeResult<GetChildResponse> GetChild(::fidl::BytePart response_buffer);

    zx_status_t GetChildRequest(::zx::channel* out_r);

    // Caller provides the backing storage for FIDL message via request and response buffers.
    zx_status_t GetChildRequest(::fidl::BytePart _response_buffer, ::zx::channel* out_r);

    // Messages are encoded and decoded in-place.
    ::fidl::DecodeResult<GetChildRequestResponse> GetChildRequest(::fidl::BytePart response_buffer);

    zx_status_t TakeChild(::zx::channel c);

    // Caller provides the backing storage for FIDL message via request and response buffers.
    zx_status_t TakeChild(::fidl::BytePart _request_buffer, ::zx::channel c);

    // Messages are encoded and decoded in-place.
    zx_status_t TakeChild(::fidl::DecodedMessage<TakeChildRequest> params);

    zx_status_t TakeChildRequest(::zx::channel r);

    // Caller provides the backing storage for FIDL message via request and response buffers.
    zx_status_t TakeChildRequest(::fidl::BytePart _request_buffer, ::zx::channel r);

    // Messages are encoded and decoded in-place.
    zx_status_t TakeChildRequest(::fidl::DecodedMessage<TakeChildRequestRequest> params);

   private:
    ::zx::channel channel_;
  };

  // Methods to make a sync FIDL call directly on an unowned channel, avoiding setting up a client.
  class Call final {
   public:

    static zx_status_t GetChild(zx::unowned_channel _client_end, ::zx::channel* out_c);

    // Caller provides the backing storage for FIDL message via request and response buffers.
    static zx_status_t GetChild(zx::unowned_channel _client_end, ::fidl::BytePart _response_buffer, ::zx::channel* out_c);

    // Messages are encoded and decoded in-place.
    static ::fidl::DecodeResult<GetChildResponse> GetChild(zx::unowned_channel _client_end, ::fidl::BytePart response_buffer);

    static zx_status_t GetChildRequest(zx::unowned_channel _client_end, ::zx::channel* out_r);

    // Caller provides the backing storage for FIDL message via request and response buffers.
    static zx_status_t GetChildRequest(zx::unowned_channel _client_end, ::fidl::BytePart _response_buffer, ::zx::channel* out_r);

    // Messages are encoded and decoded in-place.
    static ::fidl::DecodeResult<GetChildRequestResponse> GetChildRequest(zx::unowned_channel _client_end, ::fidl::BytePart response_buffer);

    static zx_status_t TakeChild(zx::unowned_channel _client_end, ::zx::channel c);

    // Caller provides the backing storage for FIDL message via request and response buffers.
    static zx_status_t TakeChild(zx::unowned_channel _client_end, ::fidl::BytePart _request_buffer, ::zx::channel c);

    // Messages are encoded and decoded in-place.
    static zx_status_t TakeChild(zx::unowned_channel _client_end, ::fidl::DecodedMessage<TakeChildRequest> params);

    static zx_status_t TakeChildRequest(zx::unowned_channel _client_end, ::zx::channel r);

    // Caller provides the backing storage for FIDL message via request and response buffers.
    static zx_status_t TakeChildRequest(zx::unowned_channel _client_end, ::fidl::BytePart _request_buffer, ::zx::channel r);

    // Messages are encoded and decoded in-place.
    static zx_status_t TakeChildRequest(zx::unowned_channel _client_end, ::fidl::DecodedMessage<TakeChildRequestRequest> params);

  };

  // Pure-virtual interface to be implemented by a server.
  class Interface {
   public:
    Interface() = default;
    virtual ~Interface() = default;
    using _Outer = Parent;
    using _Base = ::fidl::CompleterBase;

    class GetChildCompleterBase : public _Base {
     public:
      void Reply(::zx::channel c);
      void Reply(::fidl::BytePart _buffer, ::zx::channel c);
      void Reply(::fidl::DecodedMessage<GetChildResponse> params);

     protected:
      using ::fidl::CompleterBase::CompleterBase;
    };

    using GetChildCompleter = ::fidl::Completer<GetChildCompleterBase>;

    virtual void GetChild(GetChildCompleter::Sync _completer) = 0;

    class GetChildRequestCompleterBase : public _Base {
     public:
      void Reply(::zx::channel r);
      void Reply(::fidl::BytePart _buffer, ::zx::channel r);
      void Reply(::fidl::DecodedMessage<GetChildRequestResponse> params);

     protected:
      using ::fidl::CompleterBase::CompleterBase;
    };

    using GetChildRequestCompleter = ::fidl::Completer<GetChildRequestCompleterBase>;

    virtual void GetChildRequest(GetChildRequestCompleter::Sync _completer) = 0;

    using TakeChildCompleter = ::fidl::Completer<>;

    virtual void TakeChild(::zx::channel c, TakeChildCompleter::Sync _completer) = 0;

    using TakeChildRequestCompleter = ::fidl::Completer<>;

    virtual void TakeChildRequest(::zx::channel r, TakeChildRequestCompleter::Sync _completer) = 0;

  };

  // Attempts to dispatch the incoming message to a handler function in the server implementation.
  // If there is no matching handler, it returns false, leaving the message and transaction intact.
  // In all other cases, it consumes the message and returns true.
  // It is possible to chain multiple TryDispatch functions in this manner.
  static bool TryDispatch(Interface* impl, fidl_msg_t* msg, ::fidl::Transaction* txn);

  // Dispatches the incoming message to one of the handlers functions in the interface.
  // If there is no matching handler, it closes all the handles in |msg| and closes the channel with
  // a |ZX_ERR_NOT_SUPPORTED| epitaph, before returning false. The message should then be discarded.
  static bool Dispatch(Interface* impl, fidl_msg_t* msg, ::fidl::Transaction* txn);

  // Same as |Dispatch|, but takes a |void*| instead of |Interface*|. Only used with |fidl::Bind|
  // to reduce template expansion.
  // Do not call this method manually. Use |Dispatch| instead.
  static bool TypeErasedDispatch(void* impl, fidl_msg_t* msg, ::fidl::Transaction* txn) {
    return Dispatch(static_cast<Interface*>(impl), msg, txn);
  }

};

class Child final {
 public:


  class SyncClient final {
   public:
    SyncClient(::zx::channel channel) : channel_(std::move(channel)) {}

    ~SyncClient() {}

   private:
    ::zx::channel channel_;
  };

  // Methods to make a sync FIDL call directly on an unowned channel, avoiding setting up a client.
  class Call final {
   public:

  };
};

}  // namespace name
}  // namespace test

namespace fidl {

template <>
struct IsFidlType<::test::name::Parent::GetChildResponse> : public std::true_type {};
template <>
struct IsFidlMessage<::test::name::Parent::GetChildResponse> : public std::true_type {};
static_assert(sizeof(::test::name::Parent::GetChildResponse)
    == ::test::name::Parent::GetChildResponse::PrimarySize);
static_assert(offsetof(::test::name::Parent::GetChildResponse, c) == 16);

template <>
struct IsFidlType<::test::name::Parent::GetChildRequestResponse> : public std::true_type {};
template <>
struct IsFidlMessage<::test::name::Parent::GetChildRequestResponse> : public std::true_type {};
static_assert(sizeof(::test::name::Parent::GetChildRequestResponse)
    == ::test::name::Parent::GetChildRequestResponse::PrimarySize);
static_assert(offsetof(::test::name::Parent::GetChildRequestResponse, r) == 16);

template <>
struct IsFidlType<::test::name::Parent::TakeChildRequest> : public std::true_type {};
template <>
struct IsFidlMessage<::test::name::Parent::TakeChildRequest> : public std::true_type {};
static_assert(sizeof(::test::name::Parent::TakeChildRequest)
    == ::test::name::Parent::TakeChildRequest::PrimarySize);
static_assert(offsetof(::test::name::Parent::TakeChildRequest, c) == 16);

template <>
struct IsFidlType<::test::name::Parent::TakeChildRequestRequest> : public std::true_type {};
template <>
struct IsFidlMessage<::test::name::Parent::TakeChildRequestRequest> : public std::true_type {};
static_assert(sizeof(::test::name::Parent::TakeChildRequestRequest)
    == ::test::name::Parent::TakeChildRequestRequest::PrimarySize);
static_assert(offsetof(::test::name::Parent::TakeChildRequestRequest, r) == 16);

}  // namespace fidl
