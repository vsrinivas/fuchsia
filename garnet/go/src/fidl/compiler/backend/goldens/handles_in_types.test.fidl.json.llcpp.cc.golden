// WARNING: This file is machine generated by fidlgen.

#include <handles_in_types.test.fidl.json.llcpp.h>
#include <memory>

namespace llcpp {

namespace test {
namespace name {
auto ::llcpp::test::name::UnionWithHandle::which() const -> Tag {
  ZX_ASSERT(!has_invalid_tag());
  switch (ordinal()) {
  case Ordinal::kH:
    return static_cast<Tag>(ordinal());
  default:
    return Tag::kUnknown;
  }
}

void ::llcpp::test::name::UnionWithHandle::SizeAndOffsetAssertionHelper() {
  static_assert(sizeof(UnionWithHandle) == sizeof(fidl_xunion_t));
  static_assert(offsetof(UnionWithHandle, ordinal_) == offsetof(fidl_xunion_t, tag));
  static_assert(offsetof(UnionWithHandle, envelope_) == offsetof(fidl_xunion_t, envelope));
}

::llcpp::test::name::TableWithHandle::Builder TableWithHandle::Build() {
  return TableWithHandle::Builder();
}

auto ::llcpp::test::name::TableWithHandle::Builder::set_h(::zx::vmo* elem) -> Builder&& {
  ZX_ASSERT(elem);
  envelopes_[1 - 1].data = static_cast<void*>(elem);
  if (max_ordinal_ < 1) {
    max_ordinal_ = 1;
  }
  return std::move(*this);
}

}  // namespace name
}  // namespace test
}  // namespace llcpp
