# story_shell_factory test

This test executes a session shell that vends story shells with
StoryShellFactory and ensures that sessionmgr calls this interface to
attach/detach stories instead of launching separate story shell components.
