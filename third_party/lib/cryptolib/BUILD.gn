config("cryptolib_config") {
  visibility = [ ":cryptolib" ]
  include_dirs = [ "include" ]
}

module("cryptolib") {
  public_configs = [ ":cryptolib_config" ]
  sources = [
    "cryptolib.c",
  ]
  deps = [
    "//kernel/lib/libc",
  ]
}
