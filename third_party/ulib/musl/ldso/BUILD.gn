source_set("ldso") {
  configs = []

  # Use relative path to make the build relocatable
  configs += [ "..:musl_config" ]

  configs += [ "//gnbuild:no_stack_protector" ]
  sources = [
    "dlstart.c",
    "dynlink.c",
  ]
}
