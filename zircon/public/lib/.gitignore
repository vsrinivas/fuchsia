# Most subdirectories are generated by //build/zircon/populate_zircon_public.py
# but some are hand-rolled.
# This pattern will be particularly useful to set up redirections when
# migrating libraries to the GN build.
*
!.gitignore
