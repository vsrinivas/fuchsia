# Copyright 2020 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

group("unification") {
  testonly = true

  deps = [
    "images",
    "zbi",

    # Ensure the arm64 boot shims are built with the Fuchsia build
    # even though they are not used for now.
    "//zircon/kernel/target/arm64/boot-shim(//zircon/kernel/arch/arm64:physmem_arm64)",
  ]
}
