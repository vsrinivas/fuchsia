// Copyright 2018 The Fuchsia Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

library fuchsia.sys2;

// Declares a capability offered by a component to one of its children,
// such as a service offered by the component's containing realm, the component
// itself, or one of its children.
union RouteDecl {
    RoutedService service;
};

// Offers a service to a child.
struct RoutedService {
    // The name of the requested service.
    string:MAX_SERVICE_NAME_LENGTH name;

    // The provider of the service relative to the child's parent.
    // Valid relations: REALM, SELF, CHILD.
    RelativeId source;

    // TODO(CP-154): Allow specification of full source path of the service.
};
