{{>copyright comment="//"}}

use fuchsia_async as fasync;

#[fasync::run_singlethreaded]
async fn main() {
    println!("Hello, Fuchsia!");
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert!(true);
    }
}
