# Copyright 2020 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# This file was generated by the `fx create` command. The template
# is located at `//tools/create/templates/component-v1/BUILD.gn.tmpl-rust`.
# If you find something broken, we are eager to review fixes.

import("//build/package.gni")
import("//build/rust/rustc_binary.gni")
import("//build/test/test_package.gni")
import("//build/testing/environments.gni")

group("my-component-v1-rust") {
  testonly = true
  public_deps = [
    ":pkg",
    ":tests",
  ]
}

group("tests") {
  testonly = true
  public_deps = [ ":my-component-v1-rust-tests" ]
}

rustc_binary("bin") {
  name = "my-component-v1-rust"

  # Generates a GN target for unit-tests with the label `bin_test`, and
  # a binary named `my_component_v1_rust_bin_test`.
  with_unit_tests = true

  deps = [
    "//src/lib/fidl/rust/fidl",
    "//src/lib/fuchsia-async",
    "//src/lib/fuchsia-component",
    "//third_party/rust_crates:anyhow",
    "//third_party/rust_crates:futures",
  ]
}

package("pkg") {
  package_name = "my-component-v1-rust"

  deps = [ ":bin" ]

  binaries = [
    {
      name = "my_component_v1_rust"
      dest = "my-component-v1-rust"
    },
  ]

  meta = [
    # Include the package's CMX manifest.
    # The resulting component manifest can be referenced through the URL
    # fuchsia-pkg://fuchsia.com/my-component-v1-rust#meta/my-component-v1-rust.cmx.
    {
      path = rebase_path("meta/my-component-v1-rust.cmx")
      dest = "my-component-v1-rust.cmx"
    },
  ]
}

# Run with `fx test my-component-v1-rust-tests`.
test_package("my-component-v1-rust-tests") {
  deps = [ ":bin_test" ]

  tests = [
    {
      name = "my_component_v1_rust_bin_test"
      dest = "my-component-v1-rust_test"
      environments = basic_envs
    },
  ]
}
