# Copyright 2020 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# This file was generated by the `fx create` command. The template
# is located at `//tools/create/templates/component-v2/BUILD.gn.tmpl-cpp`.
# If you find something broken, we are eager to review fixes.

import("//build/package.gni")
import("//build/test/test_package.gni")
import("//build/testing/environments.gni")

group("my-component-v2-cpp") {
  testonly = true
  deps = [
    ":pkg",
    ":tests",
  ]
}

group("tests") {
  testonly = true
  deps = [ ":my-component-v2-cpp-tests" ]
}

source_set("lib") {
  sources = [
    "my-component-v2-cpp.cc",
    "my-component-v2-cpp.h",
  ]

  public_deps = [ "//zircon/public/lib/async-cpp" ]
}

executable("bin") {
  output_name = "my-component-v2-cpp"

  sources = [ "main.cc" ]

  deps = [
    ":lib",
    "//zircon/public/lib/async-default",
    "//zircon/public/lib/async-loop-cpp",
    "//zircon/public/lib/async-loop-default",
  ]
}

executable("unittests") {
  output_name = "my-component-v2-cpp_unittests"
  testonly = true

  sources = [ "my-component-v2-cpp_test.cc" ]

  deps = [
    ":lib",
    "//third_party/googletest:gtest",
    "//third_party/googletest:gtest_main",
    "//zircon/public/lib/async-default",
    "//zircon/public/lib/async-loop-cpp",
    "//zircon/public/lib/async-loop-default",
  ]
}

package("pkg") {
  package_name = "my-component-v2-cpp"

  deps = [ ":bin" ]

  binaries = [
    {
      name = "my-component-v2-cpp"
    },
  ]

  meta = [
    # Compile the package's CML manifest.
    # The resulting component manifest can be referenced through the URL
    # fuchsia-pkg://fuchsia.com/my-component-v2-cpp#meta/my-component-v2-cpp.cm.
    {
      path = rebase_path("meta/my-component-v2-cpp.cml")
      dest = "my-component-v2-cpp.cm"
    },
  ]
}

# Run with `fx test my-component-v2-cpp-tests`.
test_package("my-component-v2-cpp-tests") {
  deps = [ ":unittests" ]

  v2_tests = [
    {
      name = "my-component-v2-cpp_unittests"
      environments = basic_envs
    },
  ]
}
