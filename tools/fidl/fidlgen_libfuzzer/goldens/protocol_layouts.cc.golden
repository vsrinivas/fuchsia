// WARNING: This file is machine generated by fidlgen.

#include <lib/async-loop/cpp/loop.h>
#include <lib/async-loop/default.h>
#include <lib/fidl/cpp/fuzzing/fuzzer.h>
#include <lib/fidl/cpp/interface_ptr.h>
#include <lib/zx/channel.h>
#include <test/protocollayouts/cpp/libfuzzer.h>
#include <zircon/errors.h>
#include <zircon/syscalls.h>
#include <zircon/types.h>

using namespace ::fuzzing;
using namespace ::test::protocollayouts;

// Add //build/fuzzing:fuzzing_verbose_logging to a GN target's configs to enable.
#if FUZZING_VERBOSE_LOGGING
#include <stdio.h>
#define xprintf(fmt...) printf(fmt)
#else
#define xprintf(fmt...) \
  do {                  \
  } while (0)
#endif

extern "C" int LLVMFuzzerTestOneInput(const uint8_t* data_, size_t size_) {
  static ::async::Loop* loop_ = nullptr;

  if (loop_ == nullptr) {
    xprintf("Starting client async loop\n");
    loop_ = new ::async::Loop(&kAsyncLoopConfigAttachToCurrentThread);
  }

  // Must fuzz some protocol; first two bytes used to select protocol and method.
  if (size_ < 2) {
    xprintf("Early exit: Input too small: %zu\n", size_);
    return 0;
  }
  size_ -= 2;

  uint8_t protocol_selector_ = data_[0];
  uint8_t protocol_selection_ = protocol_selector_ % 1;

  xprintf("Starting fuzzer with %zu bytes of data\n", size_);

  // Hardcode mutually-exclusive if blocks that selects exactly one protocol.
  [[maybe_unused]] zx_status_t status_;
  if (protocol_selection_ == 0) {
#if !(defined(GOLDEN_FUZZER) || defined(PROTOCOL_test_protocollayouts_MainProtocol))
    // Selected protocol from FIDL file that is not part of this fuzzer.
    xprintf("Early exit: Chose disabled protocol: test_protocollayouts_MainProtocol\n");
    return 0;
#else

    ::fidl::InterfacePtr< ::test::protocollayouts::MainProtocol> protocol_;

    xprintf("Starting test_protocollayouts_MainProtocol service\n");
    ::fidl::fuzzing::Fuzzer< ::test::protocollayouts::MainProtocol> fuzzer_(loop_->dispatcher());
    if ((status_ = fuzzer_.Init()) != ZX_OK) {
      xprintf("Early exit: fuzzer.Init returned bad status: %d\n", status_);
      return 0;
    }

    if ((status_ = fuzzer_.BindService()) != ZX_OK) {
      xprintf("Early exit: fuzzer.BindService returned bad status: %d\n", status_);
      return 0;
    }

    if ((status_ = fuzzer_.BindClient(&protocol_, loop_->dispatcher())) != ZX_OK) {
      xprintf("Early exit: fuzzer.BindClient returned bad status: %d\n", status_);
      return 0;
    }

    FuzzInput src_(data_, size_);

    uint8_t method_selector_ = data_[1];
    uint8_t method_selection_ = method_selector_ % 15;
    if (method_selection_ == 0) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_OneWayAnonComposed))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: OneWayAnonComposed\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::imported::ComposedProtocolOneWayAnonComposedRequest>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::imported::ComposedProtocolOneWayAnonComposedRequest>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::imported::ComposedProtocolOneWayAnonComposedRequest ComposedProtocolOneWayAnonComposedRequest\n", param_size_);
      ::test::protocollayouts::imported::ComposedProtocolOneWayAnonComposedRequest ComposedProtocolOneWayAnonComposedRequest = Allocate< ::test::protocollayouts::imported::ComposedProtocolOneWayAnonComposedRequest>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.OneWayAnonComposed\n");
      protocol_->OneWayAnonComposed(std::move(ComposedProtocolOneWayAnonComposedRequest));
#endif
    }
    if (method_selection_ == 1) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_TwoWayAnonComposed))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: TwoWayAnonComposed\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedRequest>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedRequest>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedRequest ComposedProtocolTwoWayAnonComposedRequest\n", param_size_);
      ::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedRequest ComposedProtocolTwoWayAnonComposedRequest = Allocate< ::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedRequest>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.TwoWayAnonComposed\n");
      protocol_->TwoWayAnonComposed(std::move(ComposedProtocolTwoWayAnonComposedRequest), [signaller = fuzzer_.NewCallbackSignaller()](::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedResponse ComposedProtocolTwoWayAnonComposedResponse) {
        xprintf("Invoked test_protocollayouts_MainProtocol.TwoWayAnonComposed\n");
        zx_status_t status_ = signaller.SignalCallback();
        if (status_ != ZX_OK) {
          xprintf("signaller.SignalCallback returned bad status: %d\n", status_);
        }
      });
#endif
    }
    if (method_selection_ == 2) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_TwoWayAnonComposedWithError))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: TwoWayAnonComposedWithError\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedWithErrorRequest>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedWithErrorRequest>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedWithErrorRequest ComposedProtocolTwoWayAnonComposedWithErrorRequest\n", param_size_);
      ::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedWithErrorRequest ComposedProtocolTwoWayAnonComposedWithErrorRequest = Allocate< ::test::protocollayouts::imported::ComposedProtocolTwoWayAnonComposedWithErrorRequest>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.TwoWayAnonComposedWithError\n");
      protocol_->TwoWayAnonComposedWithError(std::move(ComposedProtocolTwoWayAnonComposedWithErrorRequest), [signaller = fuzzer_.NewCallbackSignaller()](::test::protocollayouts::imported::ComposedProtocol_TwoWayAnonComposedWithError_Result result) {
        xprintf("Invoked test_protocollayouts_MainProtocol.TwoWayAnonComposedWithError\n");
        zx_status_t status_ = signaller.SignalCallback();
        if (status_ != ZX_OK) {
          xprintf("signaller.SignalCallback returned bad status: %d\n", status_);
        }
      });
#endif
    }
    if (method_selection_ == 3) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_OneWayNamedComposed))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: OneWayNamedComposed\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::imported::ImportTablePayload>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::imported::ImportTablePayload>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::imported::ImportTablePayload ImportTablePayload\n", param_size_);
      ::test::protocollayouts::imported::ImportTablePayload ImportTablePayload = Allocate< ::test::protocollayouts::imported::ImportTablePayload>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.OneWayNamedComposed\n");
      protocol_->OneWayNamedComposed(std::move(ImportTablePayload));
#endif
    }
    if (method_selection_ == 4) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_TwoWayNamedComposed))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: TwoWayNamedComposed\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::imported::ImportUnionPayload>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::imported::ImportUnionPayload>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::imported::ImportUnionPayload ImportUnionPayload\n", param_size_);
      ::test::protocollayouts::imported::ImportUnionPayload ImportUnionPayload = Allocate< ::test::protocollayouts::imported::ImportUnionPayload>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.TwoWayNamedComposed\n");
      protocol_->TwoWayNamedComposed(std::move(ImportUnionPayload), [signaller = fuzzer_.NewCallbackSignaller()](::test::protocollayouts::imported::ImportTablePayload ImportTablePayload) {
        xprintf("Invoked test_protocollayouts_MainProtocol.TwoWayNamedComposed\n");
        zx_status_t status_ = signaller.SignalCallback();
        if (status_ != ZX_OK) {
          xprintf("signaller.SignalCallback returned bad status: %d\n", status_);
        }
      });
#endif
    }
    if (method_selection_ == 5) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_TwoWayNamedComposedWithError))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: TwoWayNamedComposedWithError\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::imported::ImportTablePayload>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::imported::ImportTablePayload>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::imported::ImportTablePayload ImportTablePayload\n", param_size_);
      ::test::protocollayouts::imported::ImportTablePayload ImportTablePayload = Allocate< ::test::protocollayouts::imported::ImportTablePayload>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.TwoWayNamedComposedWithError\n");
      protocol_->TwoWayNamedComposedWithError(std::move(ImportTablePayload), [signaller = fuzzer_.NewCallbackSignaller()](::test::protocollayouts::imported::ComposedProtocol_TwoWayNamedComposedWithError_Result result) {
        xprintf("Invoked test_protocollayouts_MainProtocol.TwoWayNamedComposedWithError\n");
        zx_status_t status_ = signaller.SignalCallback();
        if (status_ != ZX_OK) {
          xprintf("signaller.SignalCallback returned bad status: %d\n", status_);
        }
      });
#endif
    }
    if (method_selection_ == 6) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_OneWayImport))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: OneWayImport\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::imported::ImportTablePayload>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::imported::ImportTablePayload>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::imported::ImportTablePayload ImportTablePayload\n", param_size_);
      ::test::protocollayouts::imported::ImportTablePayload ImportTablePayload = Allocate< ::test::protocollayouts::imported::ImportTablePayload>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.OneWayImport\n");
      protocol_->OneWayImport(std::move(ImportTablePayload));
#endif
    }
    if (method_selection_ == 7) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_TwoWayImport))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: TwoWayImport\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::imported::ImportUnionPayload>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::imported::ImportUnionPayload>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::imported::ImportUnionPayload ImportUnionPayload\n", param_size_);
      ::test::protocollayouts::imported::ImportUnionPayload ImportUnionPayload = Allocate< ::test::protocollayouts::imported::ImportUnionPayload>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.TwoWayImport\n");
      protocol_->TwoWayImport(std::move(ImportUnionPayload), [signaller = fuzzer_.NewCallbackSignaller()](::test::protocollayouts::imported::ImportTablePayload ImportTablePayload) {
        xprintf("Invoked test_protocollayouts_MainProtocol.TwoWayImport\n");
        zx_status_t status_ = signaller.SignalCallback();
        if (status_ != ZX_OK) {
          xprintf("signaller.SignalCallback returned bad status: %d\n", status_);
        }
      });
#endif
    }
    if (method_selection_ == 8) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_TwoWayImportWithError))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: TwoWayImportWithError\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::imported::ImportTablePayload>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::imported::ImportTablePayload>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::imported::ImportTablePayload ImportTablePayload\n", param_size_);
      ::test::protocollayouts::imported::ImportTablePayload ImportTablePayload = Allocate< ::test::protocollayouts::imported::ImportTablePayload>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.TwoWayImportWithError\n");
      protocol_->TwoWayImportWithError(std::move(ImportTablePayload), [signaller = fuzzer_.NewCallbackSignaller()](::test::protocollayouts::MainProtocol_TwoWayImportWithError_Result result) {
        xprintf("Invoked test_protocollayouts_MainProtocol.TwoWayImportWithError\n");
        zx_status_t status_ = signaller.SignalCallback();
        if (status_ != ZX_OK) {
          xprintf("signaller.SignalCallback returned bad status: %d\n", status_);
        }
      });
#endif
    }
    if (method_selection_ == 9) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_OneWayLocal))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: OneWayLocal\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::LocalTablePayload>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::LocalTablePayload>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::LocalTablePayload LocalTablePayload\n", param_size_);
      ::test::protocollayouts::LocalTablePayload LocalTablePayload = Allocate< ::test::protocollayouts::LocalTablePayload>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.OneWayLocal\n");
      protocol_->OneWayLocal(std::move(LocalTablePayload));
#endif
    }
    if (method_selection_ == 10) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_TwoWayLocal))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: TwoWayLocal\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::LocalUnionPayload>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::LocalUnionPayload>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::LocalUnionPayload LocalUnionPayload\n", param_size_);
      ::test::protocollayouts::LocalUnionPayload LocalUnionPayload = Allocate< ::test::protocollayouts::LocalUnionPayload>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.TwoWayLocal\n");
      protocol_->TwoWayLocal(std::move(LocalUnionPayload), [signaller = fuzzer_.NewCallbackSignaller()](::test::protocollayouts::LocalTablePayload LocalTablePayload) {
        xprintf("Invoked test_protocollayouts_MainProtocol.TwoWayLocal\n");
        zx_status_t status_ = signaller.SignalCallback();
        if (status_ != ZX_OK) {
          xprintf("signaller.SignalCallback returned bad status: %d\n", status_);
        }
      });
#endif
    }
    if (method_selection_ == 11) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_TwoWayLocalWithError))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: TwoWayLocalWithError\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::LocalTablePayload>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::LocalTablePayload>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::LocalTablePayload LocalTablePayload\n", param_size_);
      ::test::protocollayouts::LocalTablePayload LocalTablePayload = Allocate< ::test::protocollayouts::LocalTablePayload>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.TwoWayLocalWithError\n");
      protocol_->TwoWayLocalWithError(std::move(LocalTablePayload), [signaller = fuzzer_.NewCallbackSignaller()](::test::protocollayouts::MainProtocol_TwoWayLocalWithError_Result result) {
        xprintf("Invoked test_protocollayouts_MainProtocol.TwoWayLocalWithError\n");
        zx_status_t status_ = signaller.SignalCallback();
        if (status_ != ZX_OK) {
          xprintf("signaller.SignalCallback returned bad status: %d\n", status_);
        }
      });
#endif
    }
    if (method_selection_ == 12) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_OneWayAnon))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: OneWayAnon\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::MainProtocolOneWayAnonRequest>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::MainProtocolOneWayAnonRequest>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::MainProtocolOneWayAnonRequest MainProtocolOneWayAnonRequest\n", param_size_);
      ::test::protocollayouts::MainProtocolOneWayAnonRequest MainProtocolOneWayAnonRequest = Allocate< ::test::protocollayouts::MainProtocolOneWayAnonRequest>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.OneWayAnon\n");
      protocol_->OneWayAnon(std::move(MainProtocolOneWayAnonRequest));
#endif
    }
    if (method_selection_ == 13) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_TwoWayAnon))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: TwoWayAnon\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::MainProtocolTwoWayAnonRequest>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::MainProtocolTwoWayAnonRequest>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::MainProtocolTwoWayAnonRequest MainProtocolTwoWayAnonRequest\n", param_size_);
      ::test::protocollayouts::MainProtocolTwoWayAnonRequest MainProtocolTwoWayAnonRequest = Allocate< ::test::protocollayouts::MainProtocolTwoWayAnonRequest>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.TwoWayAnon\n");
      protocol_->TwoWayAnon(std::move(MainProtocolTwoWayAnonRequest), [signaller = fuzzer_.NewCallbackSignaller()](::test::protocollayouts::MainProtocolTwoWayAnonResponse MainProtocolTwoWayAnonResponse) {
        xprintf("Invoked test_protocollayouts_MainProtocol.TwoWayAnon\n");
        zx_status_t status_ = signaller.SignalCallback();
        if (status_ != ZX_OK) {
          xprintf("signaller.SignalCallback returned bad status: %d\n", status_);
        }
      });
#endif
    }
    if (method_selection_ == 14) {
#if !(defined(GOLDEN_FUZZER) || defined(ALL_METHODS) || defined(METHOD_TwoWayAnonWithError))
      // Selected method from protocol that is not part of this fuzzer.
      xprintf("Early exit: Chose disabled method: TwoWayAnonWithError\n");
      return 0;
#else
      const size_t min_size_ = MinSize< ::test::protocollayouts::MainProtocolTwoWayAnonWithErrorRequest>();

      // Must have enough bytes for input.
      if (size_ < min_size_) {
        xprintf("Early exit: Input size too small: %zu < %zu\n", size_, min_size_);
        return 0;
      }

      const size_t slack_size_ = size_ - min_size_;
      const size_t slack_size_per_param = slack_size_ / 1;

      xprintf("Allocating parameters with %zu bytes (%zu bytes each)\n", slack_size_, slack_size_per_param);

      size_t param_size_;
      param_size_ = MinSize< ::test::protocollayouts::MainProtocolTwoWayAnonWithErrorRequest>() + slack_size_per_param;
      xprintf("Allocating %zu bytes for ::test::protocollayouts::MainProtocolTwoWayAnonWithErrorRequest MainProtocolTwoWayAnonWithErrorRequest\n", param_size_);
      ::test::protocollayouts::MainProtocolTwoWayAnonWithErrorRequest MainProtocolTwoWayAnonWithErrorRequest = Allocate< ::test::protocollayouts::MainProtocolTwoWayAnonWithErrorRequest>{}(&src_, &param_size_);

      xprintf("Invoking method test_protocollayouts_MainProtocol.TwoWayAnonWithError\n");
      protocol_->TwoWayAnonWithError(std::move(MainProtocolTwoWayAnonWithErrorRequest), [signaller = fuzzer_.NewCallbackSignaller()](::test::protocollayouts::MainProtocol_TwoWayAnonWithError_Result result) {
        xprintf("Invoked test_protocollayouts_MainProtocol.TwoWayAnonWithError\n");
        zx_status_t status_ = signaller.SignalCallback();
        if (status_ != ZX_OK) {
          xprintf("signaller.SignalCallback returned bad status: %d\n", status_);
        }
      });
#endif
    }

    loop_->RunUntilIdle();

    if ((status_ = fuzzer_.WaitForCallback()) != ZX_OK) {
      xprintf("fuzzer.WaitForCallback returned bad status: %d\n", status_);
    }

    protocol_.Unbind();
#endif
  }

  xprintf("Fuzzer stopped!\n");

  return 0;
}
