{{!
This template is passed two things:
  - "config" which is the JSON from the config file passed into fidldoc.
  - "syscall" which is the JSON corresponding to the system call in the FIDL intermediate
    representation.

The fidldoc.config.json file can contain a key generator_string key with a value containing
    instructions on re-running fidldoc in the correct way. }}
{{~ #if @root.config.generator_string ~}}
  <!-- {{@root.config.generator_string}} -->
{{else ~}}
  <!-- This file was generated by fidldoc, do not edit! -->
{{/if}}
# zx_{{syscall_name}}

{{docstring syscall}}
