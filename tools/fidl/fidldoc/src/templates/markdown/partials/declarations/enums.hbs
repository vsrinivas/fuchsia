## **ENUMS**
{{#each enum_declarations}}

### {{rpn name}} {{#if strict}}[strict]{{else}}[flexible]{{/if}}(/fuchsia-src/reference/fidl/language/language.md#strict-vs-flexible){:.fidl-attribute} {#{{rpn name}}}
Type: <code>{{type}}</code>

{{> filename }}

{{> doc }}

<table>
    <tr><th>Name</th><th>Value</th><th>Description</th></tr>
    {{#each members}}
        <tr id="{{rpn ../name}}.{{~ name ~}}">
            {{!-- This uses h3 since fuchsia.dev does not apply heading style if an h2/h3 is in a table --}}
            <td><h3 id="{{rpn ../name}}.{{~ name ~}}" class="add-link hide-from-toc">{{~ name ~}}</h3></td>
            <td><code>{{~ value.literal.value ~}}</code></td>
            <td>{{~> doc ~}}</td>
        </tr>
    {{/each}}
</table>
{{/each}}