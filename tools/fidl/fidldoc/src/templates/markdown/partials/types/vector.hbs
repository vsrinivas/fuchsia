{{~ #if (eq type.element_type.kind "identifier") ~}}
    vector&lt;{{~ pl type.element_type.identifier @root.name ~}}&gt;
{{~ else ~}}
    {{~ #if type.element_type.subtype ~}}
        vector&lt;{{~ type.element_type.subtype ~}}&gt;
    {{~ else ~}}
        vector&lt;{{~ type.element_type.kind ~}}&gt;
    {{~ /if ~}}
{{~ /if ~}}