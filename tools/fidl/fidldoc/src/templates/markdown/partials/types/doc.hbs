{{#if maybe_attributes ~}}
    {{~ #each maybe_attributes ~}}
        {{~ #if (eq (lower_snake_case name) "doc") ~}}
            {{~ pulldown (docLink value @root.name) ~}}
        {{~ /if ~}}
    {{~ /each ~}}
{{~ /if}}